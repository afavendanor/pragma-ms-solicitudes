spring:
  application:
    name: pragma-ms-solicitudes
    api-key: mi-api-key-secreta
  webflux:
    base-path: /applications
  profiles:
    include: r2dbc, rest-consumer, sns-sender

logging:
  level:
    co.com.pragma: DEBUG

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: swagger-ui.html
